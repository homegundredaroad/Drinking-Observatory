const root=document.documentElement;const menu=document.getElementById('menu');const nav=document.getElementById('nav');const theme=document.getElementById('theme');menu?.addEventListener('click',()=>{const open=nav.classList.toggle('open');menu.setAttribute('aria-expanded',String(open))});nav?.querySelectorAll('a').forEach(a=>a.addEventListener('click',()=>{nav.classList.remove('open');menu?.setAttribute('aria-expanded','false')}));const saved=localStorage.getItem('drinking-theme');if(saved)root.dataset.theme=saved;theme?.addEventListener('click',()=>{const next=root.dataset.theme==='light'?'dark':'light';root.dataset.theme=next;localStorage.setItem('drinking-theme',next)});const filters=[...document.querySelectorAll('[data-filter]')];const cards=[...document.querySelectorAll('#cards article')];filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;cards.forEach(c=>c.classList.toggle('hidden',f!=='all'&&!c.dataset.tags.includes(f)))}));const eras=[...document.querySelectorAll('.eras button')];eras.forEach((btn,i)=>btn.addEventListener('click',()=>{eras.forEach(b=>b.classList.remove('selected'));btn.classList.add('selected');document.getElementById('eraName').textContent=btn.textContent.toUpperCase();document.getElementById('eraTitle').textContent=btn.dataset.title;document.getElementById('eraCopy').textContent=btn.dataset.copy;const dot=document.querySelector('.eraLine i');if(dot)dot.style.left=`${i/(eras.length-1)*100}%`}));document.getElementById('random')?.addEventListener('click',()=>{const target=cards[Math.floor(Math.random()*cards.length)];target.scrollIntoView({behavior:matchMedia('(prefers-reduced-motion: reduce)').matches?'auto':'smooth',block:'center'});if(!matchMedia('(prefers-reduced-motion: reduce)').matches)target.animate([{transform:'scale(1)'},{transform:'scale(1.025)'},{transform:'scale(1)'}],{duration:700})});const reveals=[...document.querySelectorAll('.reveal')];if('IntersectionObserver'in window&&!matchMedia('(prefers-reduced-motion: reduce)').matches){const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('in');io.unobserve(e.target)}}),{threshold:.1});reveals.forEach(el=>io.observe(el))}else reveals.forEach(el=>el.classList.add('in'));const bar=document.querySelector('.progress i');addEventListener('scroll',()=>{const max=document.documentElement.scrollHeight-innerHeight;bar.style.width=`${max?scrollY/max*100:0}%`},{passive:true});