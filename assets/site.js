const filters=[...document.querySelectorAll('[data-filter]')];const cards=[...document.querySelectorAll('#cards article')];filters.forEach(btn=>btn.addEventListener('click',()=>{filters.forEach(b=>b.classList.remove('active'));btn.classList.add('active');const f=btn.dataset.filter;cards.forEach(c=>c.classList.toggle('hidden',f!=='all'&&!c.dataset.tags.includes(f)))}));const eras=[...document.querySelectorAll('.eras button')];eras.forEach(btn=>btn.addEventListener('click',()=>{eras.forEach(b=>b.classList.remove('selected'));btn.classList.add('selected');document.getElementById('eraName').textContent=btn.textContent.toUpperCase();document.getElementById('eraTitle').textContent=btn.dataset.title}));document.getElementById('random')?.addEventListener('click',()=>{const target=cards[Math.floor(Math.random()*cards.length)];target.scrollIntoView({behavior:'smooth',block:'center'});target.animate([{transform:'scale(1)'},{transform:'scale(1.025)'},{transform:'scale(1)'}],{duration:700})});